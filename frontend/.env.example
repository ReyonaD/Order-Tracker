# Base URL for the backend API. In dev, leave as /api (Vite proxies to localhost:3000).
# In production, set to your Railway backend URL, e.g. https://ordertracker-api.up.railway.app
VITE_API_URL=/api
